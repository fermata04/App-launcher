# Grid View Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a toggleable grid view mode (icon + hover overlay) alongside the existing list view, persisted across restarts.

**Architecture:** `ViewMode` enum + `AppSettings` serialized to `~/.applauncher/settings.json`; `AppGridItem` composable added to `AppListItem.kt`; `MainScreen` branches on `viewMode` to render `LazyColumn` or `LazyVerticalGrid`.

**Tech Stack:** Kotlin 1.9.21, Compose Desktop 1.5.11, kotlinx.serialization 1.6.2, Material3

> **Note:** No test framework is configured (`src/test/` doesn't exist). Verification is done by running the app with `./gradlew run` and checking UI behavior.

---

### Task 1: Add ViewMode enum and settings persistence

**Files:**
- Modify: `src/main/kotlin/com/applauncher/model/AppLauncherState.kt`

**Step 1: Add `ViewMode` enum and `AppSettings` data class**

In `AppLauncherState.kt`, add these right after the `SortMode` enum (line 20):

```kotlin
enum class ViewMode { LIST, GRID }

@kotlinx.serialization.Serializable
data class AppSettings(
    val viewMode: ViewMode = ViewMode.LIST
)
```

**Step 2: Add `_viewMode` state and `settingsFile` to `AppLauncherState`**

Inside `class AppLauncherState`, add alongside the existing fields:

```kotlin
private val settingsFile = File(System.getProperty("user.home"), ".applauncher/settings.json")

private val _viewMode = MutableStateFlow(ViewMode.LIST)
val viewMode: StateFlow<ViewMode> = _viewMode.asStateFlow()
```

**Step 3: Add `loadSettings()` and `saveSettings()` private methods**

```kotlin
private fun loadSettings() {
    try {
        if (settingsFile.exists()) {
            val settings = json.decodeFromString<AppSettings>(settingsFile.readText())
            _viewMode.value = settings.viewMode
        }
    } catch (e: Exception) {
        AppLogger.warn("Failed to load settings from ${settingsFile.path}", e)
    }
}

private fun saveSettings() {
    try {
        settingsFile.parentFile?.mkdirs()
        settingsFile.writeText(json.encodeToString(AppSettings(viewMode = _viewMode.value)))
    } catch (e: Exception) {
        AppLogger.error("Failed to save settings to ${settingsFile.path}", e)
    }
}
```

**Step 4: Add `toggleViewMode()` public method**

```kotlin
fun toggleViewMode() {
    _viewMode.value = when (_viewMode.value) {
        ViewMode.LIST -> ViewMode.GRID
        ViewMode.GRID -> ViewMode.LIST
    }
    saveSettings()
}
```

**Step 5: Call `loadSettings()` in `init {}`**

The `init` block currently reads:
```kotlin
init {
    loadApps()
    updateDerived()
}
```

Change to:
```kotlin
init {
    loadApps()
    loadSettings()
    updateDerived()
}
```

**Step 6: Verify compilation**

```bash
./gradlew compileKotlin
```

Expected: `BUILD SUCCESSFUL` with no errors.

**Step 7: Commit**

```bash
git add src/main/kotlin/com/applauncher/model/AppLauncherState.kt
git commit -m "feat: add ViewMode enum and settings persistence"
```

---

### Task 2: Add AppGridItem composable

**Files:**
- Modify: `src/main/kotlin/com/applauncher/ui/AppListItem.kt`

**Step 1: Add required imports to AppListItem.kt**

At the top of the file, add these imports (they go with the existing import block):

```kotlin
import androidx.compose.animation.core.animateFloatAsState
import androidx.compose.foundation.ExperimentalFoundationApi
import androidx.compose.ui.ExperimentalComposeUiApi
import androidx.compose.ui.input.pointer.PointerEventType
import androidx.compose.ui.input.pointer.onPointerEvent
import androidx.compose.ui.layout.ContentScale
import androidx.compose.ui.text.style.TextAlign
```

> **Note:** Some of these may already be imported. Only add missing ones. Check the existing import block first.

**Step 2: Add `AppGridItem` composable at the end of `AppListItem.kt`**

Append after the closing brace of `AppIcon`:

```kotlin
@OptIn(ExperimentalFoundationApi::class, ExperimentalComposeUiApi::class)
@Composable
fun AppGridItem(
    entry: AppEntry,
    onLaunch: () -> Unit,
    onRemove: () -> Unit,
    onEdit: () -> Unit,
    modifier: Modifier = Modifier
) {
    var showContextMenu by remember { mutableStateOf(false) }
    var isHovered by remember { mutableStateOf(false) }

    val overlayAlpha by animateFloatAsState(
        targetValue = if (isHovered) 0.6f else 0f,
        animationSpec = tween(150)
    )

    Box(
        modifier = modifier
            .size(120.dp)
            .clip(RoundedCornerShape(8.dp))
            .background(MaterialTheme.colorScheme.surfaceVariant)
            .onPointerEvent(PointerEventType.Enter) { isHovered = true }
            .onPointerEvent(PointerEventType.Exit) { isHovered = false }
            .combinedClickable(
                onClick = onLaunch,
                onLongClick = { showContextMenu = true }
            )
    ) {
        // App icon fills the cell
        AppIcon(
            path = entry.path,
            modifier = Modifier
                .fillMaxSize()
                .padding(16.dp)
        )

        // Hover overlay with app name
        Box(
            modifier = Modifier
                .fillMaxSize()
                .background(Color.Black.copy(alpha = overlayAlpha)),
            contentAlignment = Alignment.BottomCenter
        ) {
            if (overlayAlpha > 0f) {
                Text(
                    text = entry.name,
                    color = Color.White,
                    style = MaterialTheme.typography.labelMedium,
                    maxLines = 2,
                    overflow = TextOverflow.Ellipsis,
                    textAlign = TextAlign.Center,
                    modifier = Modifier
                        .fillMaxWidth()
                        .padding(horizontal = 4.dp, vertical = 6.dp)
                )
            }
        }

        // Context menu
        DropdownMenu(
            expanded = showContextMenu,
            onDismissRequest = { showContextMenu = false }
        ) {
            DropdownMenuItem(
                text = { Text("起動") },
                onClick = { showContextMenu = false; onLaunch() },
                leadingIcon = { Icon(Icons.Default.PlayArrow, contentDescription = null) }
            )
            DropdownMenuItem(
                text = { Text("編集") },
                onClick = { showContextMenu = false; onEdit() },
                leadingIcon = { Icon(Icons.Default.Edit, contentDescription = null) }
            )
            Divider()
            DropdownMenuItem(
                text = { Text("削除", color = MaterialTheme.colorScheme.error) },
                onClick = { showContextMenu = false; onRemove() },
                leadingIcon = {
                    Icon(Icons.Default.Delete, contentDescription = null,
                        tint = MaterialTheme.colorScheme.error)
                }
            )
        }
    }
}
```

**Step 3: Verify compilation**

```bash
./gradlew compileKotlin
```

Expected: `BUILD SUCCESSFUL`

**Step 4: Commit**

```bash
git add src/main/kotlin/com/applauncher/ui/AppListItem.kt
git commit -m "feat: add AppGridItem composable with hover overlay"
```

---

### Task 3: Update MainScreen

**Files:**
- Modify: `src/main/kotlin/com/applauncher/ui/MainScreen.kt`

**Step 1: Add required imports**

Add to the import block (only add missing ones):

```kotlin
import androidx.compose.foundation.lazy.grid.GridCells
import androidx.compose.foundation.lazy.grid.LazyVerticalGrid
import androidx.compose.foundation.lazy.grid.items
import com.applauncher.model.ViewMode
```

**Step 2: Collect `viewMode` state**

Inside `MainScreen`, after `val searchQuery by state.searchQuery.collectAsState()` (around line 38), add:

```kotlin
val viewMode by state.viewMode.collectAsState()
```

**Step 3: Add view toggle button to TopBar actions**

In the `actions` block of `TopAppBar`, add this button **before** the search toggle button:

```kotlin
// View mode toggle button
IconButton(onClick = { state.toggleViewMode() }) {
    Icon(
        imageVector = if (viewMode == ViewMode.GRID) Icons.Default.ViewList
                      else Icons.Default.GridView,
        contentDescription = if (viewMode == ViewMode.GRID) "リスト表示" else "グリッド表示"
    )
}
```

**Step 4: Replace the app list section with a `when(viewMode)` branch**

Find the `// アプリリスト` comment followed by `LazyColumn` (around line 262). Replace **only** the `LazyColumn` block (lines ~263–331) with:

```kotlin
// アプリリスト
when (viewMode) {
    ViewMode.LIST -> LazyColumn(
        modifier = Modifier
            .fillMaxSize()
            .padding(horizontal = 16.dp),
        verticalArrangement = Arrangement.spacedBy(8.dp),
        contentPadding = PaddingValues(vertical = 16.dp)
    ) {
        itemsIndexed(
            items = displayApps,
            key = { _, app -> app.id }
        ) { index, app ->
            val isDragging = dragState?.draggedId == app.id
            val currentDragIndex = dragState?.currentIndex ?: -1
            val isDropTarget = !isDragging &&
                dragState != null &&
                index == currentDragIndex

            AppListItem(
                entry = app,
                isDragging = isDragging,
                isDropTarget = isDropTarget,
                dragOffset = dragOffsets[app.id] ?: 0f,
                isDragEnabled = isDragEnabled,
                onLaunch = {
                    if (ProcessLauncher.launch(app)) {
                        state.recordLaunch(app.id)
                        snackbarMessage = "${app.name} を起動しました"
                    } else {
                        snackbarMessage = "${app.name} の起動に失敗しました"
                    }
                },
                onRemove = {
                    state.removeApp(app.id)
                    snackbarMessage = "${app.name} を削除しました"
                },
                onEdit = { editingApp = app },
                onDragStart = {
                    state.startDrag(app.id)
                    dragOffsets = mapOf(app.id to 0f)
                },
                onDrag = { delta ->
                    val currentOffset = (dragOffsets[app.id] ?: 0f) + delta
                    dragOffsets = dragOffsets + (app.id to currentOffset)
                    val currentAppIndex = state.getAppIndex(app.id)
                    if (currentAppIndex >= 0) {
                        val itemHeight = 72f
                        val draggedItems = (currentOffset / itemHeight).toInt()
                        val newIndex = (currentAppIndex + draggedItems).coerceIn(0, displayApps.size - 1)
                        state.updateDragPosition(newIndex)
                    }
                },
                onDragEnd = {
                    state.endDrag()
                    dragOffsets = emptyMap()
                },
                modifier = Modifier.animateItemPlacement(
                    animationSpec = spring(
                        dampingRatio = Spring.DampingRatioMediumBouncy,
                        stiffness = Spring.StiffnessMedium
                    )
                )
            )
        }
    }

    ViewMode.GRID -> LazyVerticalGrid(
        columns = GridCells.Adaptive(minSize = 120.dp),
        modifier = Modifier.fillMaxSize().padding(horizontal = 16.dp),
        verticalArrangement = Arrangement.spacedBy(8.dp),
        horizontalArrangement = Arrangement.spacedBy(8.dp),
        contentPadding = PaddingValues(vertical = 16.dp)
    ) {
        items(
            items = displayApps,
            key = { app -> app.id }
        ) { app ->
            AppGridItem(
                entry = app,
                onLaunch = {
                    if (ProcessLauncher.launch(app)) {
                        state.recordLaunch(app.id)
                        snackbarMessage = "${app.name} を起動しました"
                    } else {
                        snackbarMessage = "${app.name} の起動に失敗しました"
                    }
                },
                onRemove = {
                    state.removeApp(app.id)
                    snackbarMessage = "${app.name} を削除しました"
                },
                onEdit = { editingApp = app }
            )
        }
    }
}
```

**Step 5: Hide DropTargetArea in grid mode**

Find the `// ドロップエリア（上部）` section (around line 236). Wrap it with `if (viewMode == ViewMode.LIST)`:

```kotlin
// ドロップエリア（上部）- リスト表示時のみ
if (viewMode == ViewMode.LIST) {
    DropTargetArea(
        onActiveChange = { isDropTargetActive = it },
        modifier = Modifier
            .fillMaxWidth()
            .height(if (isDropTargetActive) 80.dp else 0.dp)
            .animateContentSize()
    ) {
        if (isDropTargetActive) {
            Box(
                modifier = Modifier
                    .fillMaxSize()
                    .padding(8.dp)
                    .clip(RoundedCornerShape(8.dp))
                    .background(DropTarget.copy(alpha = 0.2f))
                    .border(2.dp, DropTarget, RoundedCornerShape(8.dp)),
                contentAlignment = Alignment.Center
            ) {
                Text(
                    "ここにドロップしてアプリを追加",
                    color = DropTarget
                )
            }
        }
    }
}
```

**Step 6: Verify compilation**

```bash
./gradlew compileKotlin
```

Expected: `BUILD SUCCESSFUL`

**Step 7: Run the app and verify**

```bash
./gradlew run
```

Checklist:
- [ ] TopBar にグリッドアイコンボタンが表示される
- [ ] ボタンをクリックするとグリッド表示に切り替わる
- [ ] グリッドでアイコンにマウスオーバーするとアプリ名がフェードインする
- [ ] マウスが離れるとオーバーレイがフェードアウトする
- [ ] 右クリック（またはロングクリック）でコンテキストメニューが出る（起動・編集・削除）
- [ ] リスト表示に戻すと既存のリストが正常に表示される
- [ ] アプリを再起動しても選択したビューモードが維持される
- [ ] `~/.applauncher/settings.json` が作成されている

**Step 8: Commit**

```bash
git add src/main/kotlin/com/applauncher/ui/MainScreen.kt
git commit -m "feat: add grid/list view toggle to MainScreen"
```

---

## Done

After all tasks pass:

```bash
git log --oneline -5
```

Expected:
```
xxxxxxx feat: add grid/list view toggle to MainScreen
xxxxxxx feat: add AppGridItem composable with hover overlay
xxxxxxx feat: add ViewMode enum and settings persistence
xxxxxxx Add design doc for grid view with hover overlay
```
